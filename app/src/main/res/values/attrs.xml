<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="RippleView">
        <attr name="rippleColor" format="color" />
        <attr name="alphaFactor" format="float" />
        <attr name="hover" format="boolean" />
    </declare-styleable>
    <declare-styleable name="CircleTextView">
        <attr name="text" format="string"></attr>
    </declare-styleable>

    <!-- 视频分辨率宽度 -->
    <attr name="width" format="integer"/>
    <!-- 视频分辨率高度 -->
    <attr name="height" format="integer"/>
    <declare-styleable name="MovieRecorderView">
        <!-- 开始是否打开摄像头 -->
        <attr name="is_open_camera" format="boolean" />
        <!-- 一次拍摄最长时间 -->
        <attr name="record_max_time" format="integer"></attr>
        <attr name="width" />
        <attr name="height" />
    </declare-styleable>

</resources>